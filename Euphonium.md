# 项目概述

Euphonium 是一套面向动漫爱好者的本地媒体管理工具，分为两个版本：

Lite 版：纯前端 Web 应用，所有数据存储在浏览器的 IndexedDB 中，无需后端，用户可手动添加番剧、记录观看进度、撰写观后感（支持时间戳标记）、查看观看热力图，并支持数据的导入与导出。

完整版：基于 Docker 一键部署，将用户的 PC 作为媒体服务器，自动扫描本地视频文件，匹配动漫元数据（使用 Bangumi API 等），提供封面墙、剧集管理、跨设备播放（PC 作为服务端，平板/手机访问），并集成超分辨率功能（利用 Real‑CUGAN 等模型提升动漫画质）。

两个版本的数据格式保持兼容，用户可以从 Lite 版导出 JSON 文件，并在完整版中导入，实现平滑迁移。

# Lite 版功能详述

Lite 版定位为轻量级追番记录工具，用户无需安装任何后端服务，打开网页即可使用。所有数据存储在浏览器本地，用户可以完全掌控自己的数据（通过导入导出功能）。

## 番剧库管理

用户可以通过搜索 Bangumi 数据库或手动填写的方式，将喜欢的番剧添加到库中。添加时，系统会从 Bangumi 获取封面、简介、类型、总集数等元数据，用户也可以自行补充个人评分、观看状态（在看/想看/已看/搁置）、自定义标签等信息。已添加的番剧将以封面墙的形式展示，每张卡片显示封面图、标题、个人评分和状态标签，支持按状态、类型、年份等条件筛选和排序。

用户进入番剧详情页后，可以看到完整的元数据展示（包括简介、类型、首播季等），并可以随时编辑这些信息。页面上方是番剧的整体信息，中部是剧集列表（根据总集数自动生成，也支持手动增删），用户可以为每一集标记“已看”状态，记录该集的观看日期（用于后续热力图），还可以单独为某一集撰写观后感或打分。

## 观后感与时间戳

Lite 版内置一个富文本编辑器（如 Quill），用于书写番剧的整体观后感或单集感想。编辑器上有一个自定义按钮“插入时间戳”，用户可以点击后输入时间（格式 mm:ss），编辑器会在光标处插入一个可点击的时间戳标记，例如 [01:23]。这个标记在阅读时高亮显示，未来在完整版中可以与播放器联动，实现点击跳转至对应视频位置（Lite 版中仅作为视觉标记）。

## 统计与热力图

系统会根据用户的观看记录（每一集标记为“已看”时，自动记录观看日期）生成统计图表。核心统计是日历热力图（类似 GitHub 贡献图），展示用户每天观看的集数，颜色深浅代表数量多少。此外，页面还会展示累计观看集数、不同类型番剧的数量分布、个人评分分布等简单统计。所有统计基于本地 IndexedDB 数据实时计算。

## 数据导入与导出

用户可以在设置页面将全部数据（番剧、剧集、观看记录、观后感等）导出为一个 JSON 文件。该文件遵循预设的格式（包含版本号、导出时间、三张表的数据）。导入时，用户选择之前导出的 JSON 文件，系统会验证版本兼容性，清空现有数据并写入新内容。这套机制保证了用户数据完全由自己掌控，也为后续迁移到完整版提供了基础。

# 完整版功能详述

完整版在 Lite 版的基础上增加了本地媒体管理和高级特性，旨在将用户的 PC 打造为专属动漫媒体服务器。

## 本地文件扫描与自动匹配

用户首次使用或添加新番时，需要配置一个或多个存放视频文件的目录。系统会递归扫描这些目录下的视频文件（支持常见格式如 mp4、mkv、avi 等），并解析文件名（或所在文件夹名）以提取番剧名称、季度、集数等信息。随后，后端会调用 Bangumi API 进行模糊匹配，将视频文件与数据库中的番剧条目关联起来。匹配成功的视频会自动归类到对应番剧的剧集列表中，用户也可以在界面上手动修正匹配结果（例如搜索正确的番剧或调整集数对应关系）。

## 媒体库展示与播放

匹配完成后，用户可以在前端看到与 Lite 版类似的封面墙和剧集列表，但每张卡片和剧集条目都会关联到实际存在的视频文件。点击剧集的“播放”按钮，后端会通过 HTTP 流式传输视频文件，前端使用 HTML5 <video> 标签进行播放，支持拖拽进度、音量调节等标准功能。播放记录（观看时间点、是否看完）会保存到数据库，下次播放时可自动续播。

## 超分辨率（超分）处理

超分是完整版的亮点功能。用户可以在番剧详情页或剧集列表中，选择对某一集或多集发起“超分转码”任务。系统会将任务加入后台队列（使用 Celery 或类似任务队列），调用动漫优化的超分辨率模型（如 Real‑CUGAN 或 waifu2x）对视频进行逐帧处理，生成高分辨率版本。转码完成后，播放时提供“普通版”和“超分版”的切换选项。用户也可以配置在播放时实时超分（需要 GPU 支持，可选高级模式）。转码任务在后台异步执行，用户可查看进度，不影响其他操作。

## 跨设备访问与移动端体验

完整版以 Docker 容器形式部署在 PC 上，启动后在同一局域网内的其他设备（手机、平板、电视）通过浏览器访问 PC 的 IP 地址即可使用。前端界面采用响应式设计，在移动端自动适配触摸操作。此外，项目支持 PWA 技术，移动端浏览器可以将网站“添加到主屏幕”，获得接近原生应用的体验。

# 数据兼容与迁移

为了让 Lite 版用户能够平滑升级到完整版，两个版本的数据模型在设计时就保持了一致性：

Lite 版的导出 JSON 文件直接包含了 anime、episodes、watchHistory 三张表的核心字段（如标题、评分、观看状态、时间戳等）。

完整版在导入时，除了读取这些字段，还会利用 bangumi_id 重新获取最新的元数据，并补充完整版特有的字段（如文件路径、超分任务状态等）。

对于 Lite 版导出的观后感中的时间戳标记，完整版在解析后可以将其转换为可点击的跳转链接（与播放器集成），从而保留用户的笔记细节。

用户只需在完整版的设置页面上传之前导出的 JSON 文件，系统即可完成数据迁移，所有手动记录的番剧、观后感、统计历史都不会丢失。

# 非功能性需求

数据安全：Lite 版所有数据只存在于用户浏览器，完整版数据存储在用户自己指定的磁盘位置，不经过任何第三方服务器。

性能：Lite 版在数千条数据下应保持流畅，IndexedDB 查询需优化索引；完整版的视频播放与超分转码需合理利用硬件资源，避免阻塞主服务。

易用性：界面遵循直观的卡片式布局，每个操作都有明确的反馈；完整版提供 Docker 一键部署脚本，并配有详细的图文文档。

扩展性：后端 API 设计为 RESTful，前端与后端分离，便于后续增加新的元数据源（如 TMDB、AniList）或支持更多视频处理功能。
